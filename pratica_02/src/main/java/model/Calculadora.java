package model;

public class Calculadora {
    public double raizQuadrada(double n1) {
        //Método suficiente para passar na fase verde, apenas o negativo que falha
        double result = Math.sqrt(n1);
        return result;
    }
}